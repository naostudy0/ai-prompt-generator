# 機能概要

## 目的と利用場面

画像生成ツールへ入力するpositive／negativeのプロンプトを、繰り返し入力する手間を減らして準備する。
利用者は普段使う文面を登録し、画面上部で必要な項目を選び、同じ画面で結果を確認・調整して、画像生成ツールのそれぞれの入力欄へコピーする。

## 最初に作る範囲

最初の選択項目はpositive／negativeそれぞれの「デフォルト」とする。
このデフォルトは、普段使う複数のタグを含む一まとまりの文章として扱う。
他の場所で見つけたプロンプトをそのまま貼り付けやすく、編集頻度も低いため、各側に一つのテキストエリアと一つの選択ボタンを設ける。
この編集形式はデフォルト項目に適用する。今後追加する項目の入力形式は、その利用目的に合わせて設計する。

| ID | 機能 | 達成する目的 | 設計書 |
| --- | --- | --- | --- |
| F01 | デフォルトプロンプトの登録・編集・取得 | いつもの文面をまとめて登録し、再利用できる | [F01](../design/features/F01-デフォルトプロンプト.md) |
| F02 | デフォルトの選択・プロンプトの表示 | 使う文面を選び、positive／negativeの結果を得られる | [F02](../design/features/F02-プロンプトの選択と表示.md) |
| F03 | 出力の編集・個別コピー | 今回必要な文面に調整し、画像生成ツールの各欄へ貼り付けられる | [F03](../design/features/F03-出力の編集とコピー.md) |
| F04 | プロンプト全体のお気に入り登録・呼び出し | 過去に用意したプロンプトと選択内容を再利用できる | [F04](../design/features/F04-お気に入りプロンプト.md) |
| F05 | LoRA・トリガー・服装の登録と選択 | ファイル名だけに頼らずLoRAを探し、固有のトリガーと服装を準備できる | [F05](../design/features/F05-LoRAの登録と選択.md) |
| F06 | 表情・視線の登録と選択 | F08への統合前の機能IDと移行関係を記録する | [F06](../design/features/F06-表情と視線の登録と選択.md) |
| F07 | 場所・構図・動作の登録と選択 | F08への統合前の機能IDと移行関係を記録する | [F07](../design/features/F07-場所と構図と動作の登録と選択.md) |
| F08 | 名前付きオプションの登録・並べ替え・選択 | LoRA以外の候補を任意の分類・順序・選択方式で管理する | [F08](../design/features/F08-オプション項目の登録と選択.md) |
| F09 | 衣装LoRAの登録と複数選択 | 複数の衣装LoRAと各トリガーを対応付けて組み合わせられる | [F09](../design/features/F09-衣装LoRAの登録と複数選択.md) |
| F10 | セクションナビゲーションと選択概要 | 多数の入力セクションから目的の場所を見つけ、現在の選択を見失わない | [F10](../design/features/F10-セクションナビゲーションと選択概要.md) |
| F11 | 人物LoRAの差し替えコピー | 同じ衣装・構図などを複数の人物LoRAに当てたプロンプトを、選び直さず順にコピーできる | [F11](../design/features/F11-人物LoRAの差し替えコピー.md) |
| F12 | 人物LoRAの系統と系統別デフォルト | 人物LoRAに適したpositive／negativeの共通文面を自動で選び、系統ごとのタグ差を管理できる | [F12](../design/features/F12-人物LoRAの系統と系統別デフォルト.md) |
| F13 | ComfyUIへのキュー送信 | コピーと貼り付けをせず、現在のプロンプトまたは選んだ人物LoRA差し替え候補をComfyUIの実行待ちへ追加できる | [F13](../design/features/F13-ComfyUIへのキュー送信.md) |
| F14 | 人物LoRAの生成設定 | 人物LoRAごとに適したcheckpoint・sampler・schedulerを選び、ComfyUI送信時に自動適用できる | [F14](../design/features/F14-人物LoRAの生成設定.md) |

F01→F02→F03の順で設計を具体化し、実装・検証できる単位に分ける。F03のコピー動作は、画面内の出力文字列だけでも独立して検証できる。

## 確定している要件

- 個人利用とし、認証は設けない。初期機能ではデフォルトをpositive／negative各一つとして管理し、F12導入後は系統ごとにpositive／negativeを各一つ管理する。
- 初期のデフォルト文面はSeederで登録する。
- 1ページ内の上部に選択欄、下部にpositive／negativeの出力欄を設ける。
- 入力欄は初見で入力側と分かる見出しを持ち、カテゴリごとに選択ボタンを並べる。初期はデフォルトのみとする。
- 出力欄は参考画像に寄せ、上の緑系positive、下の赤茶系negativeで貼り付け先との対応を示す。
- ページロード時、各側の「デフォルト」ボタンは選択状態で始まる。
- 各デフォルトボタンの近くに編集ボタンを置き、文面を追加・編集できる。
- デフォルトの編集は各側一つのテキストエリアで行い、空の文面も許容する。空にする操作で内容を削除できる。
- 選択後にボタンを押すと、同じ画面にpositive／negativeのプロンプトを表示する。
- 出力欄は編集可能なテキストエリアとし、個別の語句を手作業で消すなどの調整ができる。
- positive／negativeはそれぞれ個別にコピーできる。
- デフォルト文面はサーバー側のSQLiteに保存し、再読み込み後も取得できる。
- 保存時は`abc, def,`の形に整え、語句間のカンマの後に半角スペースを入れ、末尾にカンマを付ける。空の文面は空のままとする。
- カンマがない改行も区切りとして補完し、連続カンマによる空の要素と同一文面の重複を除去する。特殊記法は意味を壊さず扱える範囲で整形する。
- 出力ではセクション間に空行を一つ入れ、各文面のまとまりを見分けられるようにする。
- 出力欄の直接編集では保存済みデフォルトを変更しない。
- プロンプト全体をお気に入りとして登録し、過去のものを呼び出せるようにする。F01〜F03とは別の機能単位で詳細化する。
- LoRAはファイル名と利用者が付けた登録名を保存し、一つだけ選択できるようにする。
- LoRAのトリガーは登録名と文面を持ち、一つのLoRAに複数登録できる。選択できるトリガーは一つとする。
- 服装は登録名と文面を持ち、必ず一つの人物・キャラクターLoRAに属する。選択したLoRAに属する服装だけを選べる。
- LoRAはフロント側で検索でき、複数行を常時表示する単一選択のリストから選ぶ。
- LoRAごとに0から1まで0.1刻みの推奨強度を保存し、初期値を1とする。LoRAタグは`<lora:ファイル名:強度>,`形式で出力する。
- トリガーと服装の文面には、デフォルトプロンプトと同じ整形規則を適用する。
- LoRA、トリガー、服装は追加・編集・削除できる。ファイル名の重複は禁止し、登録名の重複は許容する。LoRAを削除する場合は所属するトリガーと服装も同じ処理で削除する。
- 初期状態ではLoRA、トリガー、服装を選択しない。LoRAを選択した後は、そのLoRAに属する先頭トリガーを自動選択する。positiveの順序は、デフォルト、人物・キャラクターLoRAタグ、そのトリガー、衣装LoRAタグ、そのトリガー、服装、表示順のオプションブロックとする。
- 表情・視線・場所・構図・動作と従来オプションは、名前付きオプションブロックへ統合する。既存データは表情、視線、動作、場所、構図、従来オプションの順で移行する。
- オプションブロックは名前、単一または複数の選択方式、表示順を持つ。項目は登録名、文面、ブロック内表示順を持つ。
- ブロックはドラッグで並べ替え、項目は同一ブロック内の並べ替えと別ブロックへの移動ができる。明示的な上下移動と移動先選択も用意する。
- オプションの出力時は、保存されたブロック・項目順を使い、ブロックをまたいで完全一致するタグの重複を除去する。
- リセットでは今回の全選択とpositive／negativeの出力を空にし、LoRA強度と検索条件を初期化してLoRAセクションへ戻る。保存済みデータは変更しない。
- 衣装LoRAは既存LoRAと同じ登録情報を持ち、複数選択できる。選択した衣装LoRAごとに強度と、そのLoRAに属するトリガーを未選択または一つ指定できる。
- 衣装LoRAと既存LoRAのファイル名は全体で重複を禁止する。衣装LoRAタグとトリガーはpositiveへ出力する。
- 広い画面では中央の入力・出力領域の左側に入力セクションへのナビゲーション、右側に現在の選択概要を表示し、ページをスクロールしても参照できるようにする。
- ナビゲーションは追加・編集・並べ替えたオプションブロックへ追従する。選択概要には選択中の項目だけをセクション単位で表示し、選択解除へ即時に追従する。
- 現在のpositive／negative出力をComfyUIへ送り、設定済みワークフローを一件キューへ追加できる。
- 人物LoRA差し替え候補はチェックした候補を一括送信できる。候補は初回すべてチェックし、以後のチェック状態は同じブラウザーに保持する。全選択・全解除操作を用意する。
- ComfyUI送信ではpositive／negativeの空を許容する。通常送信は確認なし、一括送信は件数確認後に実行し、一件の失敗後も残りを処理する。
- checkpoint・sampler・schedulerの選択肢を系統ごとに管理する。人物LoRAは所属系統の各選択肢を一つずつ必須指定し、ComfyUI送信時に候補人物LoRAの設定を使用する。
- ComfyUIワークフローには系統ごとにcheckpoint・sampler・schedulerの入力位置を設定する。各役割は同じ値を複数入力へ設定できる。任意の保存先入力が設定されている場合は、人物LoRAの拡張子を含まないファイル名を使った`tmp/{fileName}`を出力先として使用する。

Seederの初期文面は[F01の初期データ](../design/features/F01-デフォルトプロンプト.md#初期データ)に定める。

## 画面構成案

配置・入力と出力の見分け方・参考画像との対応は[画面設計](../design/画面設計.md)に記載する。

```text
入力：使う項目を選ぶ
  positive → デフォルトカテゴリ → [デフォルト：選択中] [編集]
  negative → デフォルトカテゴリ → [デフォルト：選択中] [編集]

  編集中の側：positive または negative
  [デフォルト文面のテキストエリア]
  [保存] [キャンセル]

  [プロンプトを表示]

出力：画像生成ツールへコピー
  positive（緑系）   [編集可能なテキストエリア] [コピー]
  negative（赤茶系） [編集可能なテキストエリア] [コピー]
```

編集部分は編集ボタンを押したときに同じページ内で開き、保存・キャンセルで終了する。編集中は出力の表示操作を無効にする。配置と見た目は画面設計で調整する。
ボタンの表示文言「プロンプトを表示」は仮称。

## 確認事項

現在、機能全体に共通する確認事項はない。機能固有の未決事項は各詳細設計に記載する。

保存先はSQLite、出力の直接編集はデフォルトに反映しないことを確認済み（旧Q02）。
個人利用・認証なし、Seederの初期文面は確定。
出力は表示ボタンで反映し、直接手編集した出力も確認なしで作り直す。重複の完全一致比較、括弧全体の保護、Seederによる既存値の保持、空のセクションの除外、空欄のコピー無効化、複数タブでは最後の保存を優先する扱いも確定。
それ以外の設計上の提案は各設計書で「設計案」として区別する。

## お気に入りの概要

目的は、調整したプロンプト全体を保存し、後で呼び出して再利用できること。
普段使う部品としてのデフォルト文面と、出来上がったプロンプト全体を別の保存対象として扱う。
登録時点で出力欄に表示されているpositive／negativeを一組で保存し、呼び出した文面を出力欄で再編集・コピーできるようにする。あわせて登録時の選択状態と表示用の選択概要を保存し、呼び出し時には現在も存在する項目を再選択する。詳細はF04に定める。

## 将来の機能との境界

他の選択項目は、追加の要望に応じて目的・入力形式・結合順を定める。
今回の表示処理は登録文面と選択状態から文字列を用意する処理として設計する。

F12では人物LoRAの系統を管理し、従来のデフォルト文面を系統ごとに登録・編集できるpositive／negativeの二つへ拡張する。既存の両文面と人物LoRAは初期系統 Illustrious に移す。オプション項目の系統別管理は今回の範囲に含めない。

関連文書：[開発方針](開発方針.md)、[用語集](用語集.md)、[アーキテクチャ](../design/アーキテクチャ.md)。
